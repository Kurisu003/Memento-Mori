package Model;

/**
 * Enum for the different states the game can get in order to display the right window
 */
public enum GameState {
    MainMenu,
    Game,
    GameOver,
    Won,
    Shop,
    EscMenu;
}